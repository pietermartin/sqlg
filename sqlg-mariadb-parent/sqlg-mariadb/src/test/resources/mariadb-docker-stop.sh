#!/bin/bash
docker stop mariadb_main
docker stop mariadb_modern
docker stop mariadb_sink
docker stop mariadb_grateful
docker stop mariadb_empty
docker stop mariadb_g1
docker stop mariadb_g2
docker stop mariadb_readGraph
docker stop mariadb_standard
docker stop mariadb_temp
docker stop mariadb_temp1
docker stop mariadb_temp2
docker stop mariadb_subgraph
docker stop mariadb_prototype
docker stop mariadb_target